interface ProjectData {
  name: string;
  thumbnailUrl: string;
  techTags: string[];
  description: string;
  githubUrl?: string;
  liveUrl?: string;
}

interface configData {
  featuredProject: string;
  projects: string[];
}
